---
import { cn } from '@/lib/utils';

interface Props {
  for?: string;
  class?: string;
}

const { for: htmlFor, class: className } = Astro.props;
---

<label
  for={htmlFor}
  class={cn('block text-sm font-medium text-[var(--color-ink)] mb-1', className)}
>
  <slot />
</label>
