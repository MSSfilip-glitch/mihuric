# Copy to .env and fill in before deploy. Anything prefixed PUBLIC_ is
# readable at build time in the browser.

# https://web3forms.com — free inquiry form endpoint. Grab an access key
# once and paste it here; submissions are forwarded to info@mihuric.hr
# (or whatever address the key is registered to).
PUBLIC_WEB3FORMS_KEY=
